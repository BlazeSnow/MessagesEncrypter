# 常见问题

## MessagesEncrypter 是即时通讯软件吗？

不是。软件没有任何服务器，也不会发送任何消息。它只负责在本地把明文变成密文包（或把密文包还原为明文），密文包需要您通过自己的渠道（聊天软件、邮件等）传给对方。

## 与 PGP / GPG 兼容吗？

不兼容。MessagesEncrypter 使用自定义密文格式（Base64 包裹的 JSON），不读取也不生成 PGP 格式。格式完整定义见[消息格式](/protocol-v1)页面，第三方开发者可按文档自行实现互通。

## 忘记私钥密码怎么办？

私钥以该密码加密后保存在本地，软件不会存储密码（除非您主动选择记住）。忘记密码意味着对应的私钥无法解密任何历史消息，也无法找回。此时只能重新生成密钥对，并让对方改用您的新公钥。

## 密钥和密码保存在哪里？

- 密钥保存在应用本地数据目录的 `keys.db`（SQLite 数据库）中，并配有 `keys.db.sig` 完整性签名；
- 勾选「记住密码」后，私钥密码保存在 Windows 凭据管理器中，按私钥指纹区分。

所有数据仅保存在本机，不会上传到任何服务器。

## 启动时提示密钥库完整性校验失败？

软件启动时会校验密钥库文件与其签名是否匹配，不匹配则说明密钥库可能被外部修改过。常见原因包括：手动编辑过数据库文件、第三方同步或备份工具覆盖了文件、磁盘异常等。

弹窗中可以选择「退出应用」；如果确认修改来自您本人，也可以选择「忽略并重新签名」（危险操作，会重置签名密钥）。忽略后请核对密钥列表是否与您的记忆一致。

## 如何确认对方的公钥没有被调包？

在「接收方公钥」或「我的私钥」页面查看密钥指纹，并通过可信渠道（线下见面、语音通话等）与对方核对指纹一致后再使用。指纹由公钥内容计算得出，公钥有任何改动指纹都会变化。

## 为什么同样的明文每次加密出的密文都不一样？

正常现象。每次加密都会随机生成新的 AES 会话密钥和随机数，同一明文每次加密得到的密文都不同，这正是安全性的一部分。

## 为什么解密失败？

常见原因：

- 密文包复制不完整或被修改——密文被篡改时校验必然失败，此时软件不会输出任何内容；
- 选择了错误的私钥——只能用加密时所用的接收方公钥对应的私钥解密；
- 私钥密码错误。

请确认密文包完整复制（首尾没有多余字符）、选择了正确的私钥后重试。

## 删除私钥后还能解密旧消息吗？

不能。解密必须有对应的私钥，删除后无法恢复。删除前建议先导出私钥备份（导出的私钥文件仍是加密的 PEM，需要密码才能使用）。

## 如何迁移到新电脑？

在旧电脑的「我的私钥」页导出私钥（`.pem` 文件），在新电脑上导入即可；对应公钥可一并导出（`.pub` 文件）重新分享给联系人。

## 支持文件加密吗？

暂不支持。当前版本专注于文本消息的加密与解密。

## 支持哪些系统？

Windows 10（版本 17763）及以上，支持 x86、x64、ARM64 架构，通过 Microsoft Store 分发。
