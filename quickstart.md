# 快速开始

## 安装

<DownloadLinks microsoft-store="9pkn38fmmgbb" />

## 开始使用

### 第 1 步：接收方生成密钥对

1. 接收方打开软件，进入「我的私钥」页，点击「生成密钥」；
2. 设置密钥别名（方便辨认）、选择 RSA 位数（默认 4096），输入并确认私钥密码；
3. 生成完成后，列表中出现新密钥，显示别名、指纹与密钥类型（如 RSA4096）。

私钥密码是使用私钥时的凭证，请务必牢记；软件不会存储密码（除非勾选「记住密码」）。

### 第 2 步：交换公钥

1. 接收方在「我的私钥」页复制（或导出）自己的公钥，通过任意渠道发给小明；
2. 发送方在「接收方公钥」页点击「导入公钥」，粘贴公钥文本（或从文件导入），设置别名。

建议双方通过可信渠道（线下见面、语音通话等）核对密钥指纹一致，确认公钥没有被调包。

### 第 3 步：加密与解密

发送方：

1. 进入「消息加密」页，选择接收方的公钥；
2. 输入明文，点击「加密」生成密文包；
3. 把密文包通过任意渠道发给接收方。

接收方：

1. 进入「消息解密」页，选择自己的私钥；
2. 粘贴密文包，点击「解密」，输入私钥密码；
3. 得到明文。
