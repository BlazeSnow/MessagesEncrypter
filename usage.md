# 使用指南

## 消息加密

- 在「接收方」下拉框选择接收方公钥，输入待加密消息，点击「加密」生成密文包；
- 点击「复制密文」把密文包复制到剪贴板，通过聊天软件、邮件等任意渠道发送给接收方；
- 每次加密都会随机生成新的 AES-256-GCM 会话密钥，同一明文每次加密得到的密文都不同；
- 空白消息不能加密；
- 已选公钥会被记忆，下次打开软件自动恢复。

## 消息解密

- 在「我的私钥」下拉框选择私钥，粘贴密文包，点击「解密」；
- 私钥有密码时需输入密码，可勾选「记住密码」保存到 Windows 凭据管理器；
- 密文包不完整或被篡改时，解密必然失败，且不会输出任何部分内容；
- 已选私钥会被记忆，下次打开软件自动恢复。

## 接收方公钥管理

在「接收方公钥」页：

- **导入**：粘贴公钥文本，或从 `.pub` / `.pem` / `.txt` 文件导入，从文件导入时可自动用文件名填充别名；
- **复制 / 导出**：复制公钥文本，或导出为 `.pub` 文件，导出后自动打开资源管理器并选中文件；
- **重命名**：修改密钥别名；
- **删除**：确认后删除。

重复导入同一公钥时会提示指纹重复。列表按别名排序。

## 我的私钥管理

在「我的私钥」页：

- **生成**：选择 RSA 位数（2048、3072、4096、8192，默认 4096），设置私钥密码；
- **导入**：支持加密或未加密的私钥，导入后统一转为加密 PKCS#8 PEM 存储，并自动派生对应公钥；
- **复制 / 导出公钥**：得到可分享的公钥文本或 `.pub` 文件；
- **复制 / 导出私钥**：导出的私钥仍为加密状态（`.pem`），需要私钥密码才能使用；
- **修改私钥密码**：输入旧密码、新密码并确认新密码，本质是用旧密码解出私钥后再用新密码重新加密；
- **删除**：确认后删除，已保存的私钥密码会一并删除。

私钥始终以加密形式保存在本地，绝不以明文落盘。

## 私钥密码

- 生成、导入、解密、修改私钥密码时均可勾选「记住密码」，密码按私钥指纹区分，保存在 Windows 凭据管理器；
- 忘记私钥密码将无法解密对应私钥的历史消息，且无法找回；
- 修改私钥密码后可勾选记住新密码；未勾选时会删除已保存的旧密码。

## 密钥指纹

- 指纹由公钥内容计算得出（SHA-256 的前 16 字节），公钥有任何改动指纹都会变化；
- 用于线下核对公钥，防止公钥被调包；
- 密钥列表中同时显示密钥类型（如 RSA4096）与指纹。

## 设置

- **导出位置**：默认为当前用户的「下载」文件夹，可自行选择；导出密钥后会自动打开资源管理器并选中导出文件；
- **显示语言**：跟随系统、简体中文或英文，切换后需要重启软件生效；
- **软件版本**：读取自当前安装的软件包。

## 密钥库完整性

- 密钥保存在应用本地数据目录的 `keys.db`（SQLite 数据库）中，并配有 `keys.db.sig` 完整性签名；
- 软件启动时会自动校验签名，发现不匹配会弹窗提示，可选择「退出应用」或「忽略并重新签名」（危险操作）；
- 详见[常见问题](/faq)。
